version: '2'
services:
  web:
    build: .
    ports:
     - "8080:80"
     - "443:443"
  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: flightglobe
      POSTGRES_USER: flightglobe

  adminer:
    image: adminer
    ports:
      - 8081:8080